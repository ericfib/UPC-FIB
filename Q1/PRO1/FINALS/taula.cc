#include<iostream>
#include<vector>
using namespace std;

void llegeixvector(vector<int>& v, int n){
  for(int i = 0; i < n; i++){
    cin >> v[i];
  }
}

int main(){
  int n;
  cin >> n;
  vector<int> v (n);
  llegeixvector(v,n);
  while(cin >> )

  }
}
