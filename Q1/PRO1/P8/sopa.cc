#include<iostream>
#include<vector>
using namespace std;

typedef vector<vector<char> > Matriu;

void escriu_resultat(const vector<string>& v, Matriu& sopa, int x){
  bool trobat != false;
  int cont = 0;
  for (int i = v[0]; i != v[n - 1]; i++){
    for (int j = )
  }
}

int main () {
  int n, m, x;
  vector<string> seq;
  while (cin >> x >> m >> n){
    if ((x > 0) and (m > 0) and (n > 0)){
      for (int i = 0; i < x; i++){
        cin >> seq[i];
      }
      Matriu sopa(m, vector<int> (n));
      for (int i = 0; i < m; i++){
        for (int j = 0; j < n; j++){
          cin >> sopa [i][j];
        }
      }
    }
  }
}
